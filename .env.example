# Contact form. All five are required for the server action to send.
# FROM_EMAIL is both the sender and the inbox that receives new messages.
FROM_EMAIL=
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
